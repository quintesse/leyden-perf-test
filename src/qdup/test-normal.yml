name: qdup-normal-strategy

roles:
  infra:
    hosts:
      - infrahost
    setup-scripts:
      - upload_code:
        with:
          TEST_HOME: ${{INFRA_TEST_HOME}}
      - setup_infra:

    run-scripts:
      - run_infra:

    cleanup-scripts:
      - stop_infra:

  app:
    hosts:
      - apphost
    setup-scripts:
      - upload_code:
        with:
          TEST_HOME: ${{APP_TEST_HOME}}
      - setup_app:

    run-scripts:
      - apply_hw_tweaks:
        with:
          TEST_HOME: ${{APP_TEST_HOME}}
      - run_app:

    cleanup-scripts:
      - stop_app:
      - restore_hw_tweaks:
        with:
          TEST_HOME: ${{APP_TEST_HOME}}

  driver:
    hosts:
      - driverhost
    setup-scripts:
      - upload_code:
        with:
          TEST_HOME: ${{DRIVER_TEST_HOME}}
      - setup_driver:

    run-scripts:
      - apply_hw_tweaks:
        with:
          TEST_HOME: ${{DRIVER_TEST_HOME}}
      - run_driver:

    cleanup-scripts:
      - restore_hw_tweaks:
        with:
          TEST_HOME: ${{DRIVER_TEST_HOME}}
